---
title: "Design Specification: filter strip"
based-on: requirements.md v9.0.0
status: Draft
version: 6.0.0
created: "2026-08-12"
---

<!-- cspell:words setfm passthrough -->

<!-- textlint-disable
  ja-technical-writing/sentence-length,
  -->

## 1. Overview

### 1.1 Purpose

本仕様書は `filter strip` サブコマンドの **振る舞い規則** と **分類意味論** を定義します。

対象は、エクスポート済みチャットログ Markdown の冒頭にある set-frontmatter 由来の定型プロンプト部です。
これを除去し、実質的な会話内容のみを残します。

### 1.2 Scope

本仕様書が定義するのは、次の 4 点です。

- 入力ファイルを 5 つの結果 (stripped / skipped / done / passthrough / error) のいずれかへ分類する規則と、実行拒否
- 分類の評価順序と、順序を変更できない理由
- 各分類における観測可能な副作用 (ファイル書き込み・退避・退避削除)
- 実行モード (通常モードと復帰専用モード) の分岐と、それぞれで適用される規則の範囲

実装の詳細は対象外です。関数名・型・ファイル配置は Section 2.3 と Section 2.5 に限って言及します。

---

## 2. Design Principles

### 2.1 Classification Philosophy

本機能は 6000 件規模の破壊的な in-place 書き換えを伴います。
したがって分類の設計は **除去してよい根拠が積極的に確認できた場合にのみ除去する** という原則に立ちます。

この原則から次の 3 つの帰結が導かれます。

1. 判定は否定条件の連鎖として構成し、確証が得られない入力はすべて無変更で通します
2. 曖昧な入力を推測で除去せず、passthrough または error に倒します
3. 検証済みでないデータ範囲に対しては、処理を試みる前に実行そのものを拒否します

### 2.2 Design Assumptions

本仕様は次の前提のうえに成立します。前提が崩れた場合は再検証を要します。

| 前提                                                         | 根拠                                               |
| ------------------------------------------------------------ | -------------------------------------------------- |
| 定型部の終端は最初の `## Summary` 見出しで安定して判定できる | claude/2026-07 の 11671 件実測 (REQ-C-004)         |
| 定型部マーカーは `## TOPICS ASSIGNMENT RULES` である         | 正本テンプレートに存在 (REQ-F-000)                 |
| 退避ファイルと一時ファイルは走査対象に混入しない             | 走査は `*.md` を対象とし、拡張子が一致しない       |
| frontmatter は平坦なスカラー構造である                       | 実測 300 件標本で確認 (Section 5 の Edge 10)       |
| 対象は単一の agent かつ単一の年月に限定される                | REQ-C-008 が実行時に強制する                       |
| `chatlogs/` 配下へ書き込むのは本ツール群のみである           | 単一書き手前提 (REQ Section 2 Assumptions / DR-36) |

### 2.3 External Design Summary

> **Source**: Phase C の設計案と、Phase D でユーザーが確定した 2 つの決定
> (`keys()` の追加、単一ファイル出力) に基づきます。
> 8 単位への分解自体は本仕様書の提案であり、ユーザー承認を経たものではありません。

#### Feature Decomposition

| Unit             | Responsibility                                                     | REQ Coverage                                          |
| ---------------- | ------------------------------------------------------------------ | ----------------------------------------------------- |
| 引数受理ゲート   | 受理範囲外の起動を、対象を列挙する前に拒否します                   | REQ-C-008                                             |
| 境界検出         | 最初の `## Summary` と定型部マーカーの位置を求めます               | REQ-F-000, REQ-F-001, REQ-C-004                       |
| 分類             | ファイルごとに 5 つの結果のいずれかを決定します                    | REQ-F-000, REQ-F-003, REQ-F-007, REQ-F-008, REQ-F-009 |
| 安全弁           | 前提の破れと除去結果の異常を検出し、書き換えを止めます             | REQ-F-008                                             |
| 処理済み記録     | 処理済み状態 (`stripped` / `passthrough`) をキャッシュに記録します | REQ-F-009                                             |
| 退避付き書き込み | 退避と差し替えを中断耐性のある順序で実行します                     | REQ-F-004, REQ-NF-004, REQ-NF-005, REQ-C-007          |
| 退避削除         | 実行の最後に、対象ディレクトリ配下の退避を一括削除します           | REQ-F-010                                             |
| 結果報告         | 分類ごとの件数とバイト数を機械可読な形で出力します                 | REQ-F-005, REQ-F-006                                  |

#### Unit Interaction Map

```text
+------------------+
| 引数受理ゲート   |
+------------------+
         | 受理
         v
+------------------+     +------------------+     +------------------+
| 境界検出         | --> | 分類             | --> | 安全弁           |
+------------------+     +------------------+     +------------------+
                                  |                        | 合格
                          passthrough                      v
                                  |               +------------------+
                                  |               | 処理済み記録     |
                                  |               +------------------+
                                  |                        |
                                  |                        v
                                  |               +------------------+
                                  |               | 退避付き書き込み |
                                  |               +------------------+
                                  |                        |
                                  v                        v
                          +--------------------------------------+
                          | 結果報告  <--  退避削除 (実行の最後) |
                          +--------------------------------------+
```

#### Data Flow Diagram

```text
[CLI 引数] --> [引数受理ゲート] --> [対象ディレクトリ]
                     |                      |
                  拒否 v                    v
              [終了・無変更]        [対象ファイル群]
                                            |
                                            v
[ファイル] --> [境界検出] --> [分類] --> [安全弁] --> [書き込み] --> [<name>.md]
                                                          |
                                                          +------> [<name>.md.bak]
                                                                          |
                                                          [退避削除] <----+
```

<!-- ASCII diagrams only. Mermaid, PlantUML, and SVG are prohibited. -->

### 2.4 Non-Goals

> **Derivation**: すべて REQUIREMENTS の「Out of Scope」に由来します。

- ファイル単位の削除は行いません ← REQ: Out of Scope #1
- Codex プリアンブル判定によるファイル削除は行いません ← REQ: Out of Scope #2
- 定型部の再生成・復元機能は提供しません ← REQ: Out of Scope #3
- `## Summary` を持たないファイルの内容は変更しません ← REQ: Out of Scope #4
- 2 個目以降の `## Summary` 以降に出現する定型部は除去しません ← REQ: Out of Scope #5

### 2.5 Behavioral Design Decisions

| ID    | Decision                                                  | Rationale                                                                                                                                                    | Affected Rules | Status           |
| ----- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------- | ---------------- |
| DD-01 | frontmatter のキー列挙手段をクラスに追加します            | DR-14 により不要となりました。strip は frontmatter を再構築しません                                                                                          | (なし)         | Obsolete (DR-14) |
| DD-02 | 退避削除を対象ディレクトリ単位の一括削除とします          | REQ-C-008 により対象が単一ディレクトリに限定されるため、退避パスの追跡が不要です (DR-08) 。当該実行の産物でない退避の削除は DR-34 により警告として報告します | R-010, R-011   | Promoted → DR-08 |
| DD-03 | 安全弁は個別ファイル単位で作用し、実行全体を中断しません  | REQ-F-008 は「処理を継続する」と定めます。1 件の異常で残り全件の処理機会を失う設計を避けます                                                                 | R-002, R-007   | Active           |
| DD-04 | 受理範囲の検査は共通の引数解析ではなく strip 側で行います | REQ-C-008 の実装上の注意に従い、`filter` / `noise-filter` の既存挙動を変更しません                                                                           | R-001          | Active           |

#### DD-01 の検討経緯

> **本項は DR-14 により不要となりました**。strip は frontmatter を再構築しないため、
> キー列挙の手段を要しません。以下は DR-14 以前の記録です。

REQ-C-005 は「入力の既存キー順の末尾に処理済みマーカーを加えた並び順」の構築を求めます。
しかし frontmatter を保持するクラスは値の取得・設定・削除のみを公開し、キーの列挙手段を持ちません。

検討した選択肢は次の 3 つです。

- Option A: クラスにキー列挙メソッドを追加します (**採用**)
- Option B: 呼び出し側が本文分離と YAML 解析により独自にキー順を得ます
- Option C: frontmatter 生成側に追記フィールドのオプションを設けます

Option A を採る理由は、追加が小さく REQ-C-005 の文言をそのまま満たせ、他スキルからも再利用できるためです。
Option B は解析が二重化し、二重管理のリスクを負います。
Option C は「呼び出し側が並び順を構築する」という REQ-C-005 の文言とずれます。

#### DD-02 の検討経緯

当初の REQ-F-010 (v1.8.0) は削除条件を「当該実行で処理した全ファイルの `_status` が `stripped`」と定めていました。
しかし passthrough と判定されたファイルは書き込みを受けず、処理済みマーカーを持ちません。
実測では対象 6398 件に対し passthrough が 3890 件あるため、この条件はほぼ成立せず削除が発動しません。

この問題を受け、削除条件を error 0 件へ戻し、削除範囲を対象ディレクトリ単位の一括操作としました。
REQ-C-008 により対象が単一の agent・年月に限定されるため、
配下の退避はすべて strip の作業対象であり、個別追跡なしに一括削除できます。

本決定は DR-08 として正式な Decision Record に昇格し、
requirements.md も v2.0.0 で REQ-F-010 を再定義しています。
DR-06 の決定 1 (`_status` 基準) は DR-08 により破棄されました。

**Status Values:**

- `Active` — 本仕様書において現に効力を持ちます
- `Promoted → DR-xx` — 正式な Decision Record へ昇格しました
- `Obsolete (DR-xx)` — 後続の決定により不要となりました

### 2.6 Related Decision Records

> **Reference**: 次の DR は本仕様に影響します。正本は `../decision-records.md` です。

| DR-ID | Title                                                                    | Phase | Impact on This Spec                                                                                                 |
| ----- | ------------------------------------------------------------------------ | ----- | ------------------------------------------------------------------------------------------------------------------- |
| DR-01 | 除去境界は先頭から最初の `## Summary` 直前までとする                     | req   | R-006 の除去範囲を決定します                                                                                        |
| DR-02 | 出力方式は in-place + 退避とする                                         | req   | R-009 の副作用と、退避の存在を前提とする R-004 を決定します                                                         |
| DR-03 | 退避方式を Provider として抽象化し既存書き込み関数を拡張する             | req   | R-009 の操作順序と、退避の作成有無の報告を決定します                                                                |
| DR-04 | 処理済みマーカーを private フィールドとして付与する                      | req   | DR-14 により破棄。R-003 の判定材料はキャッシュに変更されました                                                      |
| DR-05 | 実装上の禁止事項を制約要件として規範化する                               | req   | R-008 の並び順明示義務の根拠です                                                                                    |
| DR-06 | 退避削除条件をマーカー基準とし、最終復旧手段を再 export とする           | req   | 決定 1 は DR-08 により破棄。復旧手段の位置づけのみ有効です                                                          |
| DR-07 | 未検証データへの適用を受理範囲の限定で強制する                           | req   | R-001 の拒否条件を決定します                                                                                        |
| DR-08 | 退避削除を対象ディレクトリ単位の一括削除とする                           | spec  | R-010 / R-011 の削除条件と削除範囲を決定します                                                                      |
| DR-09 | frontmatter を持たないファイルを error として扱う                        | spec  | R-002 の新設と Edge 15 の分類を決定します                                                                           |
| DR-10 | 退避削除の失敗を報告し、終了コードに反映する                             | spec  | R-012 の新設と Section 4.3 の削除失敗時の扱いを決定します                                                           |
| DR-11 | REQ-F-008 の到達不能な判定基準を削除する                                 | spec  | R-007 の条件が REQ-F-008 と 1 対 1 で対応することを確定します                                                       |
| DR-12 | `backupPath` の戻り値を `Promise<string>` に単純化する                   | spec  | DR-17 により破棄。R-009 の退避作成は `null` を返しうります                                                          |
| DR-13 | ファイル単位の分類を 3 つに統合する                                      | spec  | DR-15 により破棄。処理済みスキップは `done` として独立します                                                        |
| DR-14 | 処理済みマーカーをキャッシュへ移し本体を変更しない                       | spec  | R-003 の判定材料と R-009 の副作用範囲を決定します                                                                   |
| DR-15 | 処理済みスキップを `done` として独立した分類に戻す                       | spec  | Section 3.2 の分類と R-003 / R-004 の Outcome を決定します。分類と規則の 1 対 1 は DR-29 により破棄                 |
| DR-16 | 退避削除の前に退避の包含関係を検査する                                   | spec  | R-010 の条件と R-013 の新設を決定します                                                                             |
| DR-17 | 退避 Provider を `backupToBak` とし既存 `.bak` はスキップする            | impl  | R-009 の退避作成が `null` を返しうることを確定します                                                                |
| DR-23 | 孤立した退避を検出し、復帰は専用モードとして分離する                     | impl  | R-014 / R-015 の新設と実行モードの分岐を決定します                                                                  |
| DR-24 | 復帰専用モードは復帰したファイルのキャッシュエントリを削除する           | impl  | R-015 の復帰後の副作用 (キャッシュエントリの削除) を決定します                                                      |
| DR-33 | 復帰専用モードの error は報告の後に非成功終了を生成する                  | impl  | R-015 の終了コードを決定します                                                                                      |
| DR-26 | 孤立退避の対象を `.bak` に限定し `.tmp` を検出対象から外す               | impl  | 孤立退避の定義と R-015 の復帰対象を決定します                                                                       |
| DR-28 | 判定と書き込みを 1 ファイル単位へ統合し並列度を制限する                  | impl  | Section 4.2 の並列度の制限を決定します                                                                              |
| DR-29 | 判定関数を分類の単一責務とし `skipped` を分類へ加える                    | impl  | Section 3.2 の分類 5 値化・報告書式・DR-15 の 1 対 1 の破棄を決定します                                             |
| DR-30 | `StripStats` に `skipped` を設け分類と統計を 1 対 1 に対応させる         | impl  | Section 3.2 の集計を 5 分類とし、Section 4.3 の全件処理の判定式を決定します                                         |
| DR-31 | `passthrough` もキャッシュへ記録し R-003 の処理済み判定に含める          | impl  | R-003 の処理済み記録を 2 種に広げ、R-005 / R-006 成立時の記録を R-009 の外に定めます                                |
| DR-34 | 当該実行に由来しない退避も削除し、削除の前に警告として報告する           | impl  | R-010 の削除範囲の維持と、当該実行の `stripped` に由来しない退避の報告を決定します                                  |
| DR-35 | 除去範囲は splice の直前に書き込み対象の実内容と照合する                 | impl  | R-009 の書き込み前に置く再検証と、不成立時の error 計上を決定します                                                 |
| DR-36 | 読み取りから書き込みまでの競合は単一書き手前提のもとで残余として受容する | impl  | Section 4.2 の再検証 (DR-35) を競合の検出ではなく安全弁として位置づけ、競合の窓を残余として受容することを決定します |
| DR-37 | 判定 error は件数ではなく 1 件ごとにパスと規則 ID で報告する             | impl  | Section 3 の通常実行の per-file 報告の対象へ判定 error を加え、その書式を決定します                                 |
| DR-38 | 入力ディレクトリの指定を受理する (DR-07 / DR-32 の一部を上書き)          | spec  | R-001 から入力ディレクトリの拒否を外し、Edge 8 / 20 の分類を決定します                                              |
| DR-39 | 対象ディレクトリを再帰的に走査する                                       | spec  | R-017 を新設し、3 経路への同時適用と R-010 の削除範囲の拡大を決定します                                             |
| DR-40 | キャッシュキーが衝突する列挙結果を拒否する                               | spec  | R-016 を新設し、列挙直後の重複検査と実行の拒否を決定します                                                          |

### 2.7 DD to DR Promotion Criteria

**昇格を検討すべき場合:**

1. 複数の仕様やモジュールに影響します
2. 将来の設計選択を制約します
3. 有力な代替案が複数存在しました
4. 外部の関係者によるレビューを要します

**DD のまま残す場合:**

- 決定が本仕様の内部に閉じています
- 有意な代替案が存在しませんでした
- 根拠が文脈から自明です

DD-01 は共有クラスの API を拡張するため、昇格候補です。
DD-02 は要件の解釈確定にあたるため、Section 7 の確認結果しだいで昇格または要件側の修正となります。

---

## 3. Behavioral Specification

### 3.1 Input Domain

- Input Type: 対象ディレクトリ (入力ディレクトリの指定、または単一 agent・単一年月から解決) の配下 (サブディレクトリを含む) にある UTF-8 の Markdown ファイル群
- Assumptions:
  - 各ファイルは frontmatter を持つ場合と持たない場合があります
  - 改行コードは CRLF / LF のいずれもあり得ます
  - 退避ファイルと一時ファイルは走査結果に含まれません
  - 同一ファイルに対する並行実行は想定しません

### 3.2 Output Semantics

- Output Meaning: 各入力ファイルは相互に排他的な 5 つの結果のいずれかに分類されます (DR-29) 。
  加えて実行そのものが拒否される場合、いかなるファイルも分類されません。

- Possible Outcomes:

| 結果        | 意味                                                                                     | 内容の変更 | 退避の作成 |
| ----------- | ---------------------------------------------------------------------------------------- | ---------- | ---------- |
| stripped    | 定型部を除去し、処理済み状態をキャッシュに記録しました                                   | あり       | あり       |
| skipped     | 除去対象と判定しましたが、dry-run のため書き込みを見送りました (DR-29)                   | なし       | なし       |
| done        | 既に strip 済みと判定し、再処理を回避しました                                            | なし       | なし       |
| passthrough | 除去対象外と判定し、無変更で通しました (通常実行では判定をキャッシュに記録します。DR-31) | なし       | なし       |
| error       | 前提の破れまたは除去結果の異常を検出し、書き換えを見送りました                           | なし       | なし       |
| (実行拒否)  | 受理範囲外の起動であり、走査自体を行いませんでした                                       | なし       | なし       |

判定規則と分類の対応は **規則 → 分類の一方向** です (DR-29) 。
DR-15 が定めていた「分類は判定規則と 1 対 1 で対応する」は成立しません。
R-008 が `dryRun` の有無により `stripped` / `skipped` の 2 分類へ分岐するためです。

| 該当規則      | 分類                            | 意味                                      |
| ------------- | ------------------------------- | ----------------------------------------- |
| R-002 / R-007 | error                           | 前提の破れまたは除去結果の異常です        |
| R-003 / R-004 | done                            | 既に strip 済みであり、再処理を回避します |
| R-005 / R-006 | passthrough                     | 定型部を持たず、除去の対象になりません    |
| R-008         | stripped (dry-run 時は skipped) | 定型部を除去の対象と判定しました          |

除去を伴わない判定 (R-002 〜 R-007) は dry-run の有無で分類が変化しません (DR-29) 。
判定前に一律 `skipped` を返すと `done` / `passthrough` / `error` の内訳が失われ、
事前レビューの手段としての dry-run が成立しなくなるためです。

`done` と `passthrough` はいずれも内容を変更しませんが、意味が異なるため分類を分けます。
`done` は過去に除去済みであることを表し、`passthrough` は除去対象でないことを表します。
両者を統合すると、除去した実行と全件が処理済みであった実行をサマリー上で区別できません (DR-15) 。

`done` は dry-run 専用の分類ではありません。通常実行のサマリーにも計上します。

- dry-run Semantics:

dry-run 指定時は、すべての結果について内容を変更せず、退避も作成せず、
キャッシュへ処理済み状態を記録しません。`passthrough` の記録も行いません (DR-31 決定 5) 。

判定のカスケード (R-002 〜 R-008 とその順序) は通常実行と同一です。
分類ロジックは単一の判定関数に閉じなければならず、
副作用の有無のみを呼び出し側で分岐しなければなりません。
判定関数は副作用を持たず、書き込みを呼びません (DR-29 決定 2) 。
書き込むか否かは、返された分類結果を見て呼び出し側が判断します。
これにより dry-run の非破壊性が「書き込みを呼ばない」という構造で担保されます。
分類を dry-run 専用の経路で実装した場合、通常実行との判定の乖離を検出できません。

dry-run と通常実行の差異は、R-008 に到達したファイルの分類が
`stripped` ではなく `skipped` となる点に限られます (DR-29) 。
差異は表現の時制ではなく分類ラベルで表します。
`done` / `passthrough` / `error` は両モードで同一です。

dry-run の明細は、ファイルごとに次の書式で出力しなければなりません (DR-29 決定 5) 。

```text
<path>: outcome=stripped (skip)
<path>: outcome=passthrough
<path>: outcome=done
<path>: outcome=error rule=R-002
```

- `skipped` は `stripped (skip)` と表示します。他の分類に `(skip)` は付けません
- `rule=` は `error` のときのみ出力します
- 除去範囲 (`lines=`) と除去バイト数 (`removedBytes=`) は全分類で出力しません

なお除去範囲そのものは、R-009 の書き込みが用いるため判定結果の内部表現としては存続します。
出力しなくなるだけです (DR-29) 。

集計構造 (件数フィールドの集合) は両モードで一致しなければなりません。
モードによって分類が異なる場合、dry-run の結果から通常実行の結果を予測できず、
事前検証としての用途が失われます (REQ-F-005) 。
`skipped` の件数は `stripped` へは加算せず、分類と同名の件数フィールドへ計上します (DR-30) 。
分類 5 値と統計 5 件数は 1 対 1 に対応します。
通常実行では `skipped` が 0、dry-run では `stripped` が 0 となり両者は排他です。
dry-run の `skipped` は、同一入力に対する通常実行の `stripped` と一致します。

- 通常実行の per-file 報告:

通常実行では、処理したファイルを 1 件ごとに報告しなければなりません (DR-29 決定 6) 。
対象は `stripped` と `passthrough` の 2 分類とし、`done` は出力しません。
再実行時は大半が `done` となるため、全件を出力すると
その実行で実際に何が起きたかが読めなくなります。
退避付き書き込みに失敗した場合は、当該ファイルをエラーとして別途報告します。

判定が error となったファイル (R-002 / R-007) も、次の書式で 1 件ごとに報告しなければなりません (DR-37) 。

```text
error: <path> (rule=R-002)
error: <path> (rule=R-007)
error: <path> (<書き込み失敗の内容>)
```

- 判定 error と書き込み失敗はいずれも `error` としてエラー出力へ送ります。詳細部分のみが
  `rule=` と失敗内容に分かれます
- R-002 の I/O エラー変種が担ぐ `kind` / `subindex` は出力しません。dry-run 明細と粒度を揃えます
- `done` は引き続き出力しません

報告の分岐は **分類** (`outcome`) で行い、判定 (`decision.outcome`) では行いません。
書き込みに失敗したファイルは判定が `stripped` のまま分類が `error` となるため、
判定で分岐すると書き込めなかったファイルを `stripped` として報告してしまいます。

---

## 4. Decision Rules

<!--
Rule ID format: R-NNN (sequential, stable)
Rule IDs are referenced in Traceability and Edge Cases.
-->

### 4.1 実行単位の規則

評価は次の順序に従わなければなりません。

| Rule ID | Step | Condition                                                                                    | Outcome                                                                    |
| ------- | ---: | -------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| R-001   |    1 | 出力ディレクトリが指定されている、または入力ディレクトリの指定が無いまま年月が省略されている | 実行を拒否し、対象を列挙せず、1 件も変更しません                           |
| R-016   |    2 | 列挙結果に拡張子を除くベース名が同一のファイルが 2 件以上含まれる                            | 実行を拒否し、衝突したベース名と該当パスをすべて報告し、1 件も変更しません |

R-001 は対象ファイルの列挙より前に評価しなければなりません。
列挙後に拒否した場合、未検証範囲のファイル一覧が出力に現れ、拒否の意味が失われるためです。

R-001 の 2 条件は「出力ディレクトリ → 年月」の順に評価します。

入力ディレクトリ (`--input-dir` または第 1 位置引数のパス) は **受理します** (DR-38) 。
指定された場合、対象は当該パスに定まり、agent / period は対象の解決に用いられません。
したがって年月の省略も受理します。入力ディレクトリの指定が無い場合にかぎり年月を必須とします。

R-016 は列挙の直後、判定より前に評価しなければなりません。
キャッシュのキーは拡張子を除くベース名であり (Section 2.2 参照) 、R-017 の再帰走査により
異なるサブディレクトリの同名ファイルが同一エントリを共有しえます。一方が `passthrough` を
記録すると他方が R-003 で `done` となり、警告を伴わずに検査から漏れます (DR-40) 。

出力ディレクトリの指定は、`--output-dir` フラグと第 3 位置引数の双方を対象とします。
共通の引数解析はどちらも同一のフィールドへ格納するため、検査は解析後の値に対して行います。
strip は対象を in-place で書き換えるため出力先の概念を持たず、指定された値は
対象ディレクトリの解決に一切用いられません。黙って受理すると、利用者が出力先を
変えたつもりの実行が元のディレクトリを破壊的に書き換えます (DR-32) 。

#### 走査範囲 (R-017)

| Rule ID | Condition                    | Outcome                                          |
| ------- | ---------------------------- | ------------------------------------------------ |
| R-017   | 対象ディレクトリが定まった時 | 配下のサブディレクトリを含めて再帰的に走査します |

R-017 は本体の列挙・孤立退避の検出 (R-014) ・退避の一括削除 (R-010) の 3 経路すべてに
同時に適用しなければなりません。一部だけを再帰的にした場合、R-013 の包含検査が
サブディレクトリの `stripped` に対応する退避を見つけられず一括削除が恒久的に中止されるか、
未検出の孤立退避を残したまま R-011 の保持ゲートを通過して復旧材料の退避が失われます (DR-39) 。

再帰走査により、R-010 の削除範囲は対象サブツリー全体におよびます。
`stripped` が 0 件の実行では当該実行に由来する退避も存在しないため、
サブツリー内のすべての退避が DR-34 の「当該実行に由来しない退避」として報告のうえ削除されます。

#### 実行モード (R-014 / R-015)

`filter strip` は 2 つの実行モードを持ちます (DR-23) 。モードは `--recover-orphans` の指定で決まります。

| Rule ID | Condition                        | Outcome                                                        |
| ------- | -------------------------------- | -------------------------------------------------------------- |
| R-014   | `--recover-orphans` を指定しない | 通常モード。R-002 以降のすべての規則を適用します               |
| R-015   | `--recover-orphans` を指定する   | 復帰専用モード。孤立退避を復帰させて終了し、strip を行いません |

**孤立退避の定義**: 対象ディレクトリ配下に `<name>.md` が存在せず、`<name>.md.bak` が存在する状態を
指します。REQ-NF-005 の手順 2 と 3 の間で中断した場合に生じます。

`<name>.md.tmp` は検出対象に含めません (DR-26) 。REQ-NF-005 の手順 1 の時点では `<name>.md` が
無傷で存在するため、`.tmp` 単独で本体を伴わない状態は正常な処理順序では生じません。

**通常モード (R-014)** では、孤立退避を検出した場合に当該 `<name>` を error として計上し、
パスを報告します。列挙は `.md` を対象とするため、孤立退避は R-002〜R-008 のいずれにも到達しません。
検出しない場合、error が 0 件のまま R-010 の一括削除に到達し、復旧材料の退避が失われます。

**復帰専用モード (R-015)** では、次の順序で処理します。

1. R-001 の受理ゲートを評価します。対象ディレクトリの特定に `<agent> <YYYY-MM>` が必要なためです
2. 各孤立退避の `<name>.md.bak` を `<name>.md` へリネームして復帰させます
3. 復帰件数と対象パスを報告して終了します

復帰専用モードは R-002〜R-013 のいずれも評価しません。strip を行うには、利用者が復帰の結果を
確認したうえで `--recover-orphans` を指定せずに再実行します。

孤立退避の定義により復帰元は常に存在するため、検出したすべての孤立退避が復帰の対象となります。
`.bak` と `.tmp` が併存する場合、`.tmp` は検出、参照がされないため残置されます (DR-26) 。

`--dry-run` と併用された場合、復帰せず、復帰対象の件数とパスの報告にとどめます。

**終了コード (DR-33)**: 手順 3 の報告を出力した後、復帰またはキャッシュエントリ削除に失敗した
ファイルが 1 件以上ある場合は終了コードを成功以外とします。対象となる失敗は
(1) 復帰リネームの失敗、(2) 復帰後のキャッシュエントリ削除の失敗 (再試行の扱いは DR-27) の 2 種です。
報告は終了コードの生成に先行します (DR-20 決定 3) 。`--dry-run` と併用された場合はリネームと
キャッシュ削除をしないため、この失敗は生じません。

R-012 / R-013 の非成功終了が `StripStats.error` を条件としないこと (DR-20 決定 4) と
矛盾しません。DR-20 決定 4 の適用範囲は通常モード (R-014) であり、本規定は復帰専用モード
(R-015) の復帰統計を条件とします。

R-014 と R-015 は排他です。復帰専用モードでは通常の分類をしないため、孤立退避の error 計上
(R-014) は発生しません。

### 4.2 ファイル単位の規則

R-001 を通過した各ファイルについて、次の順序で評価します。

| Rule ID | Step | Condition                                                      | Outcome                         |
| ------- | ---: | -------------------------------------------------------------- | ------------------------------- |
| R-002   |    1 | frontmatter を持たない                                         | error                           |
| R-003   |    2 | キャッシュに処理済みの記録が存在する (下記の 2 種)             | done                            |
| R-004   |    3 | 対応する退避ファイルが既に存在する                             | done                            |
| R-005   |    4 | 本文に `## Summary` が 1 つも存在しない                        | passthrough                     |
| R-006   |    5 | 本文先頭から最初の `## Summary` 直前までに定型部マーカーが無い | passthrough                     |
| R-007   |    6 | 除去後の本文が空である、または除去率が 99% を超える            | error                           |
| R-008   |    7 | 上記すべてに該当しない                                         | stripped (dry-run 時は skipped) |

R-008 のみが dry-run の有無により分類を分岐します (DR-29) 。
R-002 〜 R-007 の分類は両モードで同一です。

R-003 が処理済みとみなす記録は、次の 2 種です (DR-31 決定 2) 。

| status        | 記録された意味                         | 記録した規則       |
| ------------- | -------------------------------------- | ------------------ |
| `stripped`    | 除去した内容の書き込みが成立しました   | R-008              |
| `passthrough` | 除去対象を持たないと判定が確定しました | R-005 または R-006 |

いずれも「判定が確定しており再判定を要さない」記録であるため、同じく `done` とします。
`passthrough` を除外すると、除去対象を持たないファイルを実行のたびに読み直すため、
記録の目的である再処理の回避が成立しません (DR-31) 。

順序の変更は許されません。根拠は次のとおりです。

- R-002 を先頭に置く理由は 2 つあります。
  frontmatter の欠落はパイプラインの前提が破れた状態であり、
  後続の判定を試みる前に検出すべきものです。
  なお R-003 の処理済み判定はキャッシュを参照するため (DR-14 / DR-31) 、frontmatter に依存しません。
- R-003 を R-004 より先に置く理由は、退避が削除された後も冪等性を保つためです。
  退避のみを根拠にすると、R-010 による削除後の再実行で strip 済みファイルが再処理されます。
  R-010 は対象ディレクトリ配下の退避を一括削除するため、
  退避を唯一の判定材料とすると再実行時に判定材料が失われます。
- R-005 を R-006 より先に置く理由は、`## Summary` が無ければ除去範囲を定義できず、
  マーカーの探索範囲そのものが定まらないためです。
- R-007 を R-008 より先に置く理由は、安全弁が書き込みの前段でなければ機能しないためです。

#### 境界検出の規則 (REQ-C-004)

R-005 と R-006 が用いる検出は、次の規則に従わなければなりません。

| 対象           | 検出規則                                                         |
| -------------- | ---------------------------------------------------------------- |
| `## Summary`   | 行頭完全一致 (`^## Summary$`) の **最初の出現** を境界とします   |
| 定型部マーカー | 行頭完全一致 (`^## TOPICS ASSIGNMENT RULES$`) の出現を確認します |

**Markdown を構文解析しません**
コードフェンス内・引用内・リスト内・ユーザー発話本文内であるかは解釈しません。

この単純化は claude/2026-07 の実測に基づきます (REQ-C-004) 。

| 検証項目                                             | 実測                         |
| ---------------------------------------------------- | ---------------------------- |
| 定型部保有ファイルで最初の `## Summary` がフェンス内 | 0 件                         |
| 引用・インデント付きの定型部マーカー                 | 0 件                         |
| `## Summary` 見出しの表記ゆれ                        | 3 件 (いずれも strip 対象外) |

構文解析を採らない理由は、パーサの導入が依存と解釈差分を増やし、
破壊的処理のリスクをむしろ高めるためです。
解釈の余地を排した単純規則のほうが、誤 strip の可能性を低く保てます。

マーカーの存在確認だけでは定型部であることを証明できませんが、
本仕様は保守側に倒す設計により誤 strip を回避します。
マーカーが最初の `## Summary` より後ろにある場合は passthrough とします (Edge 13)。
本文が偶然 `## Summary` で始まる場合もマーカー不在により passthrough となります (Edge 12)。

両文字列は実装ファイルに直書きせず、定数として定義しなければなりません。
テンプレート変更時の変更箇所を 1 箇所に閉じるためです。

**前提が破れる条件**: `set-frontmatter` の定型部テンプレート、
または `filter` の削除パターンが変更された場合は再検査を要します (REQ-C-004) 。
未検証のデータセットへの適用は R-001 が実行時に拒否します。

#### 除去率の定義

R-007 の除去率は次の式で算出します。

```text
除去率 = 除去バイト数 ÷ 本文バイト数
```

本文バイト数は frontmatter を除いた部分の長さです。
frontmatter の長さに左右されず、除去範囲そのものの比率を評価するためです。

なお REQ-F-001 の Rationale が示す「定型部はファイルサイズの平均 77.8%」は
ファイル全体を分母とする別の指標です。
実測では両者の差は小さく、除去対象 6398 件において
本文分母の平均が 77.60%、ファイル分母の平均が 77.26% です。

#### 除去率の実測分布 (claude/2026-07)

除去対象 6398 件における除去率 (本文分母) の分布は次のとおりです。

| 区間      | 件数 |
| --------- | ---- |
| 50 - 79%  | 4435 |
| 80 - 89%  | 1919 |
| 90 - 94%  | 42   |
| 95 - 98%  | 2    |
| 99 - 100% | 0    |

| 統計値 | 本文分母 |
| ------ | -------- |
| 平均   | 77.60%   |
| 中央値 | 75.90%   |
| p95    | 87.28%   |
| p99    | 89.62%   |
| 最大   | 96.23%   |

**現データセットにおいて R-007 の除去率条件は発火しません。**
最大が 96.23% であり、閾値の 99% まで 2.77 ポイントの余裕があります。
除去後の本文が空になるファイルも 0 件です。

この事実は 2 つの意味を持ちます。

第一に、閾値 99% は現データセットに対して **安全側に十分な余裕** を持ちます。
正常な入力を error に倒す誤検出は発生しません。

第二に、閾値 99% は **上側の異常しか検出しません**。
分布の最大が 96.23% であることは、境界判定が壊れて本文の大半を除去した場合でも、
除去率が 99% に達しなければ検出されないことを意味します。
安全弁として機能するのは事実上「除去後の本文が空になる」条件のみです。

閾値を下げる案は採りません。
95% とした場合、実測 2 件が error となりますが、
それらが誤削除であるか正常であるかを判別する根拠がありません。
error は R-011 により退避の一括削除を止めるため、
根拠なく閾値を下げると正常な実行が毎回中断します。

除去率は「異常の可能性を示す指標」であり、
誤削除そのものを判別する手段ではありません。
誤削除の防止は R-005 / R-006 の境界検出が担い、
R-007 は判定ロジックが破綻した場合の最終的な安全弁として位置づけます。

R-008 が成立したファイルに対する副作用は、次の順序で発生しなければなりません。

| Rule ID | Step | Action                                   |
| ------- | ---: | ---------------------------------------- |
| R-009   |    1 | 除去後の内容を一時ファイルへ書き出します |
| R-009   |    2 | 元ファイルを退避先へ移動します           |
| R-009   |    3 | 一時ファイルを元のファイル名へ移動します |

この順序により、いかなる中断点でも元の内容または完全な除去後の内容の一方が必ず残ります。
手順 2 と 3 の間で中断した場合、元のファイル名は存在せず、退避に元の内容が残ります。

#### 除去範囲の書き込み直前の再検証 (DR-35)

手順 1 で除去後の内容を組み立てる前に、除去範囲が書き込み対象の実内容と整合することを
検証しなければなりません。

R-008 の除去範囲は判定時に読み取った内容を基準に確定します。
書き込みは同一ファイルを改めて読み取るため、2 回の読み取りの間に内容が差し替わると、
行番号は古い内容を基準としたまま新しい内容へ適用されます。
行番号による除去は範囲を暗黙にクランプするため、この不整合は例外として現れません。

検証は次の 4 点とします。すべて成立した場合にのみ除去を適用します。

| 検証項目           | 条件                                                           |
| ------------------ | -------------------------------------------------------------- |
| frontmatter の存在 | 読み取った内容が frontmatter を持つ                            |
| 開始辺             | `removalStartLine` が読み取った内容の frontmatter 行数と等しい |
| 終了辺             | `removalEndLine + 1` 行目が `## Summary` と行頭完全一致する    |
| 範囲の順序         | `removalEndLine` が `removalStartLine` 以上である              |

開始辺と終了辺の 2 点により、除去範囲は「本文先頭から境界見出しの直前まで」(R-008 / DR-01) と
一意に定まります。frontmatter は構成上除去の対象から外れ、REQ-F-002 が実行時に強制されます。

frontmatter の存在は独立した検証としなければなりません。
これを frontmatter 行数 `-1` として表すと、除去範囲を持たない分類の `removalStartLine` (`-1`) と
一致します。除去範囲を持たない判定を担った呼び出しが素通りします。

frontmatter 行数の算出は R-008 の除去範囲の構築と同一の手段を用いなければなりません (REQ-C-001) 。
開始辺の照合は両者が同じ定義に立つことを前提として成立します。

いずれかが成立しない場合、当該ファイルを書き換えず error として計上しなければなりません。
例外を送出してはなりません (DD-03) 。error の計上により R-011 が退避の一括削除を止めます。

この検証は R-009 の手順 1 の直前に置かれ、手順 3 のスワップ地点ではありません。
そのため読み取りから書き込みまでの間に対象が差し替わる競合の窓は閉じません。
この窓は `chatlogs/` 配下の単一書き手前提 (Section 2.2 Design Assumptions) のもとで
残余として受容します (DR-36) 。本検証は競合の検出ではなく、壊れた frontmatter と
不整合な除去範囲に対する安全弁として維持します。

#### 並列度の制限 (DR-28 決定 3)

ファイル単位の評価と副作用は並行して実行してよいものとしますが、
同時に処理するファイル数には上限を課さなければなりません。
上限は実行設定として与え、無制限の一括起動を行ってはなりません。

6000 件規模の対象に対して全件を同時に起動すると、
ファイルディスクリプタと I/O が枯渇し、実行そのものが破綻するためです。
なお R-009 の手順 1 〜 3 の順序は 1 ファイル内の制約であり、
並列度の制限とは独立に常に成立しなければなりません。

処理済み状態の記録はキャッシュに対して行い、本体の frontmatter は変更しません (DR-14) 。
frontmatter を再構築しないため、キー順の明示的な構築を要しません。

#### passthrough の記録 (DR-31 決定 1・4)

R-005 または R-006 が成立したファイルについても、処理済み状態をキャッシュに記録しなければなりません。
記録する status は `passthrough` とし、`rule` には成立した規則 (R-005 または R-006) を記録します。

この記録は R-009 の副作用の範囲外です。`passthrough` は本体を変更せず退避も作成しないため、
R-009 の 3 段の書き込み手順に乗りません。記録は分類が確定した直後に、書き込みとは独立に行います。

記録に失敗した場合、当該ファイルは error として計上しなければなりません (DR-31 決定 3) 。
本体は無傷ですが、`stripped` と扱いを変えると記録の失敗が成功として報告される経路が生じます。
error に計上すれば R-011 により退避も保持され、安全側に倒れます。

#### frontmatter の不変性 (AC-024)

strip 済みファイルの frontmatter は strip 前と **同一の内容** でなければなりません。

判定はバイト単位ではなく、`ChatlogFrontmatter` による同一性比較で行います。
CRLF での入力が `normalizeLine` による `LF` に正規化されるため、バイト単位の一致が機能しません (REQ-NF-003 / Edge 11)。
改行コードの正規化は決定事項であり、これを取りやめません。

同一性は次の条件で判定します。

| 条件       | 内容                                                                          |
| ---------- | ----------------------------------------------------------------------------- |
| キー集合   | 両者のキー集合が一致すること                                                  |
| 値         | 各キーについて値が一致すること (`string` は `===`、`string[]` は長さと各要素) |
| キーの順序 | **比較対象に含めません**                                                      |

キーの順序を含めない理由は、strip が frontmatter を再構築せず順序が物理的に不変であるためです。
順序を判定条件に含めると、将来 frontmatter を経由する処理が入った際に過剰な制約となります。

比較を呼び出し側でインラインに実装してはなりません (REQ-C-001) 。
文字列表現の比較に依存すると、改行コード・引用符・インデントの差異が
内容の変化として誤検出されます。

### 4.3 実行終了時の規則

全ファイルの評価を終えた後、次の順序で評価します。

| Rule ID | Step | Condition                                                                             | Outcome                                                                    |
| ------- | ---: | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| R-010   |    1 | error が 0 件であり、dry-run が指定されておらず、かつ退避の包含関係が成立する (R-013) | 対象ディレクトリ配下の退避を一括削除します                                 |
| R-011   |    2 | error を計上したファイルが 1 件以上ある                                               | 退避をすべて保持します                                                     |
| R-012   |    3 | R-010 の削除で失敗した退避が 1 件以上ある                                             | 失敗件数と対象パスを報告し、終了コードを成功以外とします                   |
| R-013   |    4 | 退避の包含関係が成立しない                                                            | 退避を保持し、不足する退避のパスを報告します。終了コードを成功以外とします |

R-010 の削除範囲は **対象ディレクトリ配下 (サブディレクトリを含む) の退避ファイル全件** です (DD-02 / R-017)。

#### 当該実行に由来しない退避の報告 (R-010)

削除範囲は全件であるため、当該実行が作成していない退避も削除されます。
削除の **前** に、退避一覧のうち期待退避パスの集合に含まれないものを、件数と各パスで警告として報告します (DR-34)。
期待退避パスは `stripped` と判定したパスから `<name>.md.bak` として構成します (R-013 と同じ構成方法)。

報告は「前回実行の中断により残った退避」と「strip 以外の経路で置かれた退避」を区別しません。
実行終了時の規則が受け取るのは `stripped` と判定したパス集合のみであり、
両者を判別する材料を持たないためです。

本報告は R-010 〜 R-013 の判定に影響せず、終了コードも変えません。
削除そのものは全件に対して行います。

#### 退避の包含関係 (R-013)

削除の前に、次の包含関係が成立しなければなりません (DR-16) 。

```text
{ stripped と判定したファイルのパス } ⊆ { 存在する退避のパス }
```

包含が成立しない場合、`stripped` と計上したにもかかわらず対応する退避を持たないファイルが
存在することを意味します。
当該ファイルは復旧手段を持たないまま本体が書き換わった状態であり、
この状態で残りの退避を一括削除すると被害が拡大します。

**件数の比較では代替できません。**
退避の件数が `stripped` の件数と一致しない経路が 3 つあるためです。

| 経路                            | 影響                                                     |
| ------------------------------- | -------------------------------------------------------- |
| 前回実行が error で終了した     | R-011 により退避が全保持され、次回は `done` となり計上外 |
| Phase 4 と Phase 5 の間での中断 | 退避は存在するが `stripped` に計上されていない           |
| 他プロセス・手動作業由来の退避  | 仕様は退避の作成元を区別しません                         |

いずれも正常な状態であり、件数の等式で異常と判定するのは誤りです。
件数の下限を検査する形も、余剰と欠損が相殺するため採りません。

検査に用いるパスは判定の結果として既に保持しており、追加の追跡を要しません。
退避の一覧は削除のために取得するものと同一であり、追加の走査も生じません。
REQ-C-008 により対象は単一の agent・年月のディレクトリに限定されるため、
配下の退避はすべて strip の作業対象であり、退避パスを個別に追跡する必要がありません。

前回実行の中断により残った退避も削除対象に含みます。
当該実行が全件を error なく処理し終えた時点で、対象ディレクトリは正常な strip 済み状態であり、
古い退避を保持する理由が無いためです。

passthrough と done の件数は削除の可否に影響しません。
判定材料は error の有無と、退避の包含関係 (R-013) の 2 つです。

#### 退避削除と処理済み判定の関係

R-004 は既存の退避を処理済みの根拠として用いる一方、R-010 はその退避を削除します。
一見すると、保護判定に用いた証拠を実行の最後に消す設計のように見えます。

この矛盾は R-003 を R-004 より先に置く順序制約により解消されます (Section 4.2) 。
処理済みの一次的な判定材料はキャッシュであり、退避ではありません (DR-14) 。
キャッシュが健在である限り、退避が削除された後も再実行は冪等です。

退避は二次的な判定材料であり、キャッシュを持たないファイルに対する保険として機能します。

**縮退挙動**: キャッシュが失われた場合 (別環境での実行・キャッシュの削除・
`DEFAULT_CACHE_ROOT` の変更) 、R-003 と R-004 が同時に無効となります。
このとき strip 済みファイルは再評価されます。

再評価の結果は次のとおりです。

| 対象                                          | 再評価の結果                                   |
| --------------------------------------------- | ---------------------------------------------- |
| 通常の strip 済みファイル                     | R-006 により passthrough。本文は破壊されません |
| 先頭 strip 後もなお定型部が残るもの (Edge 14) | 再び R-008 に到達し、再 strip されます         |

前者が安全である理由は、strip 済みファイルの本文が `## Summary` から始まり、
その手前に定型部マーカーを持たないためです。

後者は実測 11671 件中 0 件であり、現データセットでは該当しません。
ただし定型部マーカーが 2 個目以降の `## Summary` 以降に位置する構造は原理的に起こりえます。
DR-14 は、発生した場合の損失を許容しています。
一次ソースは `~/.claude/projects/` の JSONL であり、re-export による復元が可能です (DR-06) 。

本仕様は R-010 の削除範囲を変更しません。
削除範囲を「当該実行が作成した退避」に限定する案は、退避パスの個別追跡を要し、
DD-02 が REQ-C-008 を根拠に不要と判断した設計を覆します。
守られる対象は同じ 2 件であり、追跡の複雑さに見合いません。

この判断の見直しは Open Question 4 として記録します。

ただし削除の可否判断には R-013 の包含検査を課します (DR-16) 。
キャッシュの健在を前提とするだけでなく、削除の直前に退避の整合性を実行時に確かめます。

#### 全件処理の判定 (DR-15 / DR-30)

全ファイルの評価を終えた時点で、次の式が成立しなければなりません。

```text
stripped + skipped + done + passthrough == total  かつ  error == 0
```

分類が相互に排他的であるため (Section 3.2) 、
この 2 条件により「全ファイルが異常なく評価を完了した」ことを表現できます。

式が成立しない場合、評価されなかったファイルが存在するか、
分類の計上に漏れがあることを示します。

孤立退避 (R-014) は `.md` を持たず列挙されないため `total` に含まれません。当該 error は左辺の
等式には現れず、`error == 0` の条件のみで捕捉されます。2 条件の連言であるため判定は成立します。

復帰専用モード (R-015) では分類をしないため、本判定式を評価しません。

dry-run 指定時は退避自体を作成しないため、削除も行いません。

削除は実行の最後に一括で行わなければなりません。
ファイルごとに逐次削除した場合、途中失敗により「一部だけ削除済み」の状態が生じます。

#### 削除失敗時の扱い (R-012)

一括削除であっても、個別の削除操作は失敗しえます (DR-10) 。
Windows 環境ではファイルロックやアンチウイルスによる占有が想定され、
6000 件規模では一部失敗が現実的に起こります。

削除は全件について試行しなければなりません。
1 件の失敗で残りの削除を中断した場合、削除済みと未削除の混在範囲が実行ごとに変わり、
再実行時の状態把握が困難になります。

削除に失敗した退避が 1 件以上ある場合、失敗件数と対象パスを報告します。
この場合の終了コードは成功以外とします。
削除失敗を報告しないと、退避が残存したか削除されたかを利用者が判別できず、
ディスク使用量の見積もりと次回実行時の前提が崩れます。

削除失敗は Section 3.2 の分類には加えません。
分類は入力ファイル単位の結果であり、実行単位の後処理である削除とは粒度が異なるためです。
また削除失敗は、既に確定したファイル単位の分類結果を取り消しません。
削除は全ファイルの評価を終えた後の後処理であり、その失敗は書き換えの正しさに影響しないためです。

削除に失敗した退避は、次回実行時の一括削除で再び対象となります。

---

## 5. Edge Cases

| #  | Input                                            | Classification | REQ                  | Rationale                                                                                                                                 |
| -- | ------------------------------------------------ | -------------- | -------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| 1  | `## Summary` を持つが定型部マーカーを持たない    | passthrough    | REQ-F-000            | 実測 3890 件が該当します。除去すると実コンテンツを破壊します                                                                              |
| 2  | `## Summary` を 1 つも持たない                   | passthrough    | REQ-F-003            | 実測 266 件が該当します。除去範囲を定義できません                                                                                         |
| 3  | 退避ファイルが既に存在する                       | done           | REQ-F-007            | 処理済みスキップ。再 strip すると退避を除去後の内容で上書きし、元ログを失います                                                           |
| 4  | キャッシュに処理済み記録があり、退避を持たない   | done           | REQ-F-009            | 処理済みスキップ。記録は `stripped` / `passthrough` のいずれでもよく (DR-31) 、退避削除後の再実行にあたりキャッシュが唯一の判定材料です   |
| 5  | 除去後の本文が空になる                           | error          | REQ-F-008            | 判定ロジックの誤りが疑われます。書き換えを見送り被害を局所化します                                                                        |
| 6  | 除去率が 99% を超える                            | error          | REQ-F-008            | 実測平均は 77.8% です。逸脱は境界判定の異常を示唆します                                                                                   |
| 7  | 年月を省略して起動する                           | 実行拒否       | REQ-C-008            | agent 直下の全年月が走査対象となり、未検証データに処理がおよびます                                                                        |
| 8  | 入力ディレクトリを指定して起動する               | 受理           | REQ-C-008            | 対象は当該パスに定まります。agent と年月は対象の解決に用いられません (DR-38)                                                              |
| 9  | frontmatter に未知フィールドを持つ               | stripped       | REQ-C-005            | 並び順を明示しなければ未知フィールドが失われます                                                                                          |
| 10 | frontmatter がネスト構造やブロックスカラーを含む | (前提外)       | REQ-NF-003           | 実測標本では出現しません。将来 frontmatter が構造化した場合は再検証を要します                                                             |
| 11 | 入力の改行コードが CRLF である                   | stripped       | REQ-NF-003           | 検出は改行コードに依存しません。書き出し時に LF へ正規化されます。frontmatter の不変性はバイト単位ではなく同一性比較で判定します (AC-024) |
| 12 | 本文中のユーザー発話が偶然 `## Summary` で始まる | passthrough    | REQ-F-000            | マーカーの確認により保護されます。実例が 1 件確認されています                                                                             |
| 13 | 定型部が最初の `## Summary` より後ろにある       | passthrough    | REQ-F-000, REQ-C-004 | 実測 4 件が該当します。先頭アンカー方式では対象外です                                                                                     |
| 14 | 先頭 strip 後もなお定型部が残る                  | stripped       | REQ-F-009            | 実測 0 件。マーカーが 2 個目以降の `## Summary` 以降にある場合に発生しえます。再実行時はキャッシュにより done となり再処理を防ぎます      |
| 15 | frontmatter を持たない                           | error          | REQ-F-008            | export の出力は frontmatter を必ず持つため、欠落は前提の破れを示します                                                                    |
| 16 | 出力ディレクトリを指定して起動する               | 実行拒否       | REQ-C-008            | 値は対象の解決に用いられず、出力先を変えたつもりの実行が元のディレクトリを書き換えます (DR-32)                                            |
| 17 | 判定の確定後に対象ファイルの内容が差し替わる     | error          | REQ-F-008            | 除去範囲が現在の内容と整合しません。書き換えを見送り error として計上します (DR-35)                                                       |
| 18 | 対象がサブディレクトリを持つ                     | 再帰走査       | REQ-C-009            | classify 済みの対象はプロジェクト別サブディレクトリへ移動しており、非再帰では 0 件になります (DR-39)                                      |
| 19 | ベース名が重複するファイルが列挙される           | 実行拒否       | REQ-C-010            | キャッシュキーが衝突し、一方が誤って done と判定されます (DR-40)                                                                          |
| 20 | 入力ディレクトリを指定し年月を省略して起動する   | 受理           | REQ-C-008            | 対象は当該パスに定まるため、年月を要しません (DR-38)                                                                                      |

Edge 10 は現時点で発生しない前提です。
前提が崩れた場合の挙動は本仕様では規定せず、Section 7 に記録します。

Edge 15 を passthrough ではなく error とする根拠を示します。
`originalLogs/` 配下の全ファイルは `export-chatlogs` の `renderMarkdown` により生成されます。
frontmatter のデリミタと `session_id` / `date` は条件分岐の外にあり、無条件に出力されます。
該当箇所は `export-chatlogs/scripts/libs/session-writer.ts` の 57 行目から 62 行目です。
claude / codex / chatgpt の 3 エクスポータはいずれもこの経路を通り、他に本番の書き出し経路はありません。
したがって frontmatter の欠落は「対象外の入力」ではなく「パイプラインの前提が破れた状態」であり、
黙って通すのではなく検出して報告します。

---

## 6. Requirements Traceability

| Requirement ID | Spec Rule                                            | Notes                                                                                                                                                             |
| -------------- | ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| REQ-F-000      | R-006, Edge 1/12/13                                  | マーカー確認を除去の前提条件とします                                                                                                                              |
| REQ-F-001      | R-006, R-008                                         | 除去範囲は本文先頭から最初の `## Summary` 直前までです                                                                                                            |
| REQ-F-002      | R-008                                                | 既存キー順の保持は並び順の明示により担保されます                                                                                                                  |
| REQ-F-003      | R-005, Edge 2                                        | `## Summary` 不在時は無変更で通します                                                                                                                             |
| REQ-F-004      | R-009, R-014, R-015                                  | 退避は書き込みと不可分の順序で作成されます。孤立退避の検出と復帰は R-014 / R-015 が定めます                                                                       |
| REQ-F-005      | Section 3.2 dry-run Semantics                        | dry-run は同一のカスケードで分類のみを行い副作用を生じません。R-008 到達分は `skipped` として明細に出力します (DR-29)                                             |
| REQ-F-006      | Section 3.2, R-010, R-012                            | 統計 5 分類の件数・退避削除の結果と失敗に加え、通常実行では `stripped` / `passthrough` と判定 error を 1 件ごとに報告します (DR-29 / DR-30 / DR-37)               |
| REQ-F-007      | R-004, Edge 3                                        | 既存退避の存在を処理済みの根拠とします                                                                                                                            |
| REQ-F-008      | R-002, R-007, R-009, Edge 5/6/15/17                  | 安全弁は個別ファイル単位で作用します。R-009 の書き込み直前の再検証も安全弁に含みます                                                                              |
| REQ-F-009      | R-003, R-005, R-006, R-008, Edge 4/14                | 判定順序の上位に置き、退避削除後の冪等性を担保します。`passthrough` の記録は R-005 / R-006 の成立直後に行います (DR-31)                                           |
| REQ-F-010      | R-010, R-011, R-012, R-013                           | 削除範囲は対象ディレクトリ単位です (DR-08)。削除失敗は R-012、包含関係の破れは R-013 が扱います。当該実行に由来しない退避は削除の前に警告として報告します (DR-34) |
| REQ-F-011      | (DR-14 により不要)                                   | strip は本体の frontmatter を変更しないため、private フィールドが存在しません                                                                                     |
| REQ-NF-001     | Section 2.3                                          | 既存の近接実装と同一の主処理構成を採ります                                                                                                                        |
| REQ-NF-002     | R-005, R-006                                         | 境界検出は副作用を持たない判定として定義されます                                                                                                                  |
| REQ-NF-003     | Edge 11, Section 4.2 frontmatter の不変性            | 改行コードに依存しない検出を求めます。書き出し時の正規化は AC-024 の判定基準に影響します                                                                          |
| REQ-NF-004     | R-009                                                | 一時ファイル経由により原子性を担保します                                                                                                                          |
| REQ-NF-005     | R-009, R-014, R-015                                  | 3 段階の操作順序を規定します。手順 2〜3 間の中断で生じる孤立退避を R-014 が検出します                                                                             |
| REQ-C-001      | Section 2.3                                          | 既存実装の優先利用を前提とします                                                                                                                                  |
| REQ-C-002      | Section 2.3                                          | 見出し検出の配置方針に従います                                                                                                                                    |
| REQ-C-003      | (impl 以降)                                          | 開発プロセスへの制約であり、振る舞い規則を持ちません                                                                                                              |
| REQ-C-004      | Section 4.2 境界検出の規則, R-005, R-006, Edge 12/13 | 行頭完全一致による検出を規定し、構文解析を用いません                                                                                                              |
| REQ-C-005      | (DR-14 により不要)                                   | frontmatter を再構築しないため、`fieldOrder` の明示を要しません                                                                                                   |
| REQ-C-006      | (DR-14 により不要)                                   | private フィールドを本体に付与しないため、ホワイトリスト制約が不要です                                                                                            |
| REQ-C-007      | R-009                                                | 退避を伴う書き込みの手段を限定します                                                                                                                              |
| REQ-C-008      | R-001, Edge 7/8/16/20                                | 出力ディレクトリの指定と対象不明な起動を列挙前に拒否し、入力ディレクトリの指定を受理します                                                                        |
| REQ-C-009      | R-017, Edge 18                                       | 本体列挙・孤立退避の検出・退避の一括削除の 3 経路を同時に再帰化します                                                                                             |
| REQ-C-010      | R-016, Edge 19                                       | 列挙直後にキャッシュキーの衝突を検査し、成立時は実行を拒否します                                                                                                  |

---

## 7. Open Questions

> **Status**: INCOMPLETE

| # | Question                                                                                                                      | Source                    | Impact                                                                                                                          |
| - | ----------------------------------------------------------------------------------------------------------------------------- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| 1 | issue cle-2rf の Design と受け入れ基準が DR-02 と矛盾したままです。issue 側の更新はいつ行いますか                             | requirements.md Q-01      | 仕様と課題管理の記述が乖離し続けます                                                                                            |
| 2 | frontmatter が将来ネスト構造を持った場合の挙動を規定すべきですか                                                              | Edge 10 / REQ-NF-003      | 現時点では発生しませんが、規定が無いと将来の変更で沈黙して壊れます                                                              |
| 3 | ~~除去率 99% の閾値は設定可能とすべきですか、固定値でよいですか~~ **決着**: 固定値のまま据え置きます (Section 4.2 の実測分布) | REQ-F-008 / codex risk    | 実測最大 96.23% であり誤検出は発生しません。閾値を下げる根拠も無く、下げると正常な実行が R-011 により中断します                 |
| 4 | キャッシュ喪失後の再実行において、Edge 14 に該当するファイルが再 strip され本文を失うことを許容し続けますか                   | codex risk 前提 3 / DR-14 | 現データセットでは実測 0 件です。将来テンプレートが変わり該当が生じた場合、R-010 の削除範囲または退避の追跡方式の変更を要します |

---

## 8. Change History

<!-- SemVer: MAJOR = behavior removed / redefined, MINOR = spec item added,
     PATCH = clarification only. Keep frontmatter `version` equal to the newest row.
     `based-on` must cite a three-part version that exists in requirements.md.
     See deckrd-rule-document-versioning.md -->

| Date       | Version | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ---------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-08-12 | 1.0.0   | Initial specification (based-on requirements.md v2.0.0 / DR-08 反映済み)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-08-12 | 1.1.0   | explore レビュー (F-01 / A-01) を反映。frontmatter 欠落を error とする R-002 を新設し、以降の規則 ID を繰り下げ (旧 R-002〜R-010 → 新 R-003〜R-011)。Edge 15 を passthrough から error へ変更。除去率の算出式を明記。based-on を v2.1.0 へ更新                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 2026-08-12 | 1.1.1   | R-002 / Edge 15 の根拠として DR-09 を Section 2.6 に追加。振る舞い規則の変更は伴わない                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-08-12 | 1.2.0   | spec harden レビュー (DR-10 / DR-11 / DR-12) を反映。削除失敗を扱う R-012 を新設し Section 4.3 に扱いを追記 (DR-10)。R-010 の条件に dry-run 未指定を明示 (W-01)。Section 2.6 に DR-10 〜 DR-12 を追加。Traceability の REQ-F-006 / REQ-F-010 を更新し、あわせて分類数の表記を 5 分類から 4 分類へ訂正。based-on を v3.0.0 へ更新                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-08-12 | 2.0.0   | DR-13 を反映し、ファイル単位の分類を 4 つから 3 つへ統合。skipped を passthrough に統合し R-003 / R-004 の Outcome を変更 (規則自体は存置)。Section 3.2 に passthrough の 2 経路と内訳報告を追加。Section 4.3 に全件処理の判定式を追加。Edge 3 / 4 / 14 の分類を更新。Section 2.6 に DR-13 を追加                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-08-12 | 3.0.0   | DR-14 を反映し DR-04 を破棄。処理済み状態の保持先を本体の frontmatter から `ChatlogCache` へ移し、strip は本体の frontmatter を変更しない。R-003 の条件をキャッシュ参照に変更し、R-009 からマーカー付与を削除。DD-01 を Obsolete とし、Traceability の REQ-F-011 / REQ-C-005 / REQ-C-006 を不要として記録。based-on を v5.0.0 へ更新                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-08-13 | 3.1.0   | DR-15 を反映し DR-13 を破棄。分類を 3 つから 4 つへ戻し、処理済みスキップを `done` として独立させる。R-003 / R-004 の Outcome と Edge 3 / 4 / 14 を更新 (判定規則そのものは不変)。Section 3.2 に dry-run Semantics を新設し、キャッシュ非記録・単一判定関数・集計構造の一致を規定 (F-04)。全件処理の判定式を 4 項へ変更。Section 2.6 に DR-15 を追加。based-on を v5.1.0 へ更新                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 2026-08-13 | 3.1.1   | codex risk レビューの危険な前提 1 / 2 に対応。REQ-C-004 が定める境界検出の規則 (行頭完全一致・構文解析を行わない・実測根拠・定数化の義務) を Section 4.2 に明記。要件の追加・変更は伴わない。あわせて Section 4.2 に残存した DR-04 由来のキー順構築の記述を DR-14 / AC-024 に合わせて訂正。Traceability の REQ-C-004 を更新                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-08-13 | 3.1.2   | codex risk レビューの危険な前提 3 に対応 (A-1)。Section 4.3 に「退避削除と処理済み判定の関係」を新設し、R-004 と R-010 の見かけ上の矛盾が R-003 の順序制約により解消されること、およびキャッシュ喪失時の縮退挙動 (Edge 14 の 2 件が再 strip される) を明記。R-010 の削除範囲は変更しない。Open Question 4 を追加                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 2026-08-13 | 3.2.0   | DR-16 を反映。R-010 の削除条件に退避の包含関係を追加し、破れた場合を扱う R-013 を新設。Section 4.3 に「退避の包含関係」を追記し、件数比較で代替できない理由を記録。削除範囲は不変 (DD-02 維持)。Section 2.6 に DR-16 を追加。Traceability の REQ-F-010 を更新                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-08-13 | 3.2.1   | 再実測により実測値を訂正。対象 6402→6398 / passthrough 3888→3890。Edge 14 と Section 4.3 の縮退挙動における「2 件 (0.02%)」を実測 0 件へ訂正し、構造上ありうる旨に改める。振る舞い規則の変更は伴わない。based-on を v5.2.1 へ更新                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 2026-08-13 | 3.3.0   | codex risk レビューの対応候補 B に対応。Section 4.2 に除去率の実測分布 (claude/2026-07 の 6398 件) を追加し、R-007 の除去率条件が現データセットで発火しないこと、閾値 99% が上側の異常しか検出しないこと、閾値を下げない根拠を明記。Open Question 3 を決着済みとして記録                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-08-13 | 3.4.0   | 対応候補 C を反映。Section 4.2 に「frontmatter の不変性 (AC-024)」を新設し、判定をバイト単位から同一性比較へ改める。Edge 11 と Traceability の REQ-NF-003 を更新。based-on を v5.3.0 へ更新                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2026-08-13 | 3.4.1   | based-on を requirements.md v5.3.1 へ更新 (`_status` 陳腐化記述の一掃に追随)。仕様の変更は伴わない                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 2026-08-13 | 3.5.0   | DR-17 を Section 2.6 に追加し、DR-12 を破棄済みとして注記。R-009 の退避作成は `backupToBak` が担い、既存 `.bak` に対しては `null` を返しうる (strip の経路では R-004 により到達しない)。based-on を requirements.md v6.0.0 へ更新。R-001〜R-013 の振る舞い規則は不変                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-08-13 | 3.6.0   | DR-23 を反映。Section 4.1 に実行モードの規則 R-014 / R-015 を新設し、孤立退避（`.md` を伴わない `.bak` / `.tmp`）の定義・通常モードでの error 計上・`--recover-orphans` による復帰専用モードを規定。復帰専用モードは R-002〜R-013 を評価せず復帰のみで終了する。Section 1.2 の Scope に実行モードを追加、Section 4.3 の全件処理の判定にモード別の扱いを追記、Section 2.6 に DR-23、Traceability の REQ-F-004 / REQ-NF-005 を更新。R-001〜R-013 の振る舞い規則は不変                                                                                                                                                                                                                                                                                                                                             |
| 2026-08-16 | 3.7.0   | DR-26 を反映。孤立退避の定義から `<name>.md.tmp` を除外し、`.bak` が存在する状態のみを孤立退避とする。R-015 の復帰対象から「`.tmp` のみが残る場合は報告のみ」の規定を削除し、検出したすべての孤立退避が復帰対象となることを明示。REQ-NF-005 の手順 1 では `<name>.md` が無傷であり `.tmp` 単独の孤立が正常な処理順序では生じないことを根拠として付記。Section 2.6 に DR-26 を追加。R-001〜R-013 の振る舞い規則は不変                                                                                                                                                                                                                                                                                                                                                                                            |
| 2026-08-16 | 4.0.0   | DR-29 を反映し DR-15 の「分類は判定規則と 1 対 1 で対応する」を破棄 (MAJOR: behavior redefined)。Section 3.2 の分類を 4 つから 5 つへ広げ `skipped` (除去対象だが dry-run のため書き込みを見送った状態) を追加。対応表を「規則 → 分類」の一方向へ改め、R-008 のみが `dryRun` により `stripped` / `skipped` へ分岐する旨を Section 4.2 のカスケード表に明記。dry-run と通常実行の差異を「表示上の時制」から分類ラベルへ改訂し、明細の書式 (`outcome=` 形式・`rule=` は error のみ・`lines=` / `removedBytes=` 全廃) を規定。判定関数が副作用を持たないこと、通常実行の per-file 報告 (`stripped` / `passthrough` のみ) を追加。DR-28 決定 3 の並列度の制限を Section 4.2 へ規範化。Section 2.6 に DR-28 / DR-29 を追加し Traceability の REQ-F-005 / REQ-F-006 を更新。based-on を requirements.md v7.0.0 へ更新 |
| 2026-08-16 | 5.0.0   | DR-30 を反映し DR-29 決定 3 のうち `skipped` の件数を `StripStats.stripped` へ加算する部分を破棄 (MAJOR: decided approach discarded)。Section 3.2 dry-run Semantics の集計を「`skipped` は `stripped` へ加算し統計は 4 分類」から「分類 5 値と統計 5 件数が 1 対 1 に対応し `stripped` / `skipped` は排他」へ改訂し、非対称が意図的である旨の記述を削除。Section 4.3 の全件処理の判定式を `stripped + skipped + done + passthrough == total` へ改訂。Section 2.6 に DR-30 を追加し Traceability の REQ-F-006 を 5 分類へ更新。based-on を requirements.md v8.0.0 へ更新。R-001〜R-015 の振る舞い規則は不変                                                                                                                                                                                                      |
| 2026-08-16 | 5.1.0   | DR-31 を反映 (MINOR: 振る舞いを追加)。Section 4.2 の R-003 の処理済み記録を `stripped` / `passthrough` の 2 種として表に明示し、`passthrough` を含める理由を記録。R-005 / R-006 成立時の記録を「passthrough の記録」として新設し、R-009 の副作用の外で判定確定直後に行うこと・`rule` に成立規則を記録すること・記録失敗を error に計上することを規定。Section 3.2 の passthrough 行と dry-run Semantics に記録の有無を明記。Section 2.2 の処理済み記録の責務、Section 2.6 に DR-31、Edge 4 と Traceability の REQ-F-009 を更新。based-on を requirements.md v8.1.0 へ更新。R-001〜R-015 の規則 ID と評価順序は不変                                                                                                                                                                                              |
| 2026-08-16 | 5.1.1   | `based-on` を requirements.md v8.2.0 へ更新 (PATCH: 参照のみ)。REQ-F-006 のバイト数具体化に伴う上流版数の追従であり、本書の振る舞い規定に変更はない                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-08-16 | 5.2.0   | DR-32 を反映 (MINOR: 振る舞いを追加)。Section 4.1 の R-001 の条件に「出力ディレクトリが指定されている」を追加し、3 条件の評価順序を「入力ディレクトリ → 出力ディレクトリ → 年月」と規定。`--output-dir` と第 3 位置引数の双方が同一フィールドへ入るため検査は解析後の値に対して行うこと、strip が in-place のため出力先の概念を持たないことを明記。Section 5 に Edge 16 を追加し Traceability の REQ-C-008 を更新。based-on を requirements.md v8.3.0 へ更新。R-002〜R-015 の規則と評価順序は不変                                                                                                                                                                                                                                                                                                               |
| 2026-08-16 | 5.3.0   | DR-33 を反映 (MINOR: 振る舞いを追加)。Section 4.1 の R-015 に復帰専用モードの終了コードを規定し、復帰リネームの失敗とキャッシュエントリ削除の失敗が 1 件以上あるとき報告の後に終了コードを成功以外とすることを明示。報告が終了コードの生成に先行すること (DR-20 決定 3) 、`--dry-run` 併用時は当該失敗が生じないこと、DR-20 決定 4 の適用範囲が通常モード (R-014) に限られ本規定と矛盾しないことを併記。Section 2.6 に DR-24 と DR-33 を追加。based-on を requirements.md v8.4.0 へ更新。R-001〜R-014 の振る舞い規則は不変 (R-015 に終了コードを追加)                                                                                                                                                                                                                                                           |
| 2026-08-16 | 5.4.0   | DR-34 を反映 (MINOR: 振る舞いを追加)。Section 4.3 に「当該実行に由来しない退避の報告 (R-010)」を新設し、削除の前に期待退避パスの集合に含まれない退避を件数とパスで警告報告することを規定。報告が前回中断の残骸と外部由来を区別しないこと、R-010 〜 R-013 の判定と終了コードに影響しないことを明記。DD-02 の Rationale と Traceability の REQ-F-010 に当該報告を追記し、Section 2.6 に DR-34 を追加。based-on を requirements.md v8.5.0 へ更新。削除範囲と R-001〜R-015 の振る舞い規則は不変                                                                                                                                                                                                                                                                                                                     |
| 2026-08-18 | 5.5.0   | DR-35 を反映 (MINOR: 振る舞いを追加)。Section 4.2 に「除去範囲の書き込み直前の再検証」を新設し、R-009 の手順 1 の前に frontmatter の存在・開始辺 (`removalStartLine` = frontmatter 行数)・終了辺 (`removalEndLine + 1` 行目が `## Summary`)・範囲の順序の 4 点を検証すること、不成立なら書き換えず error に計上し例外を送出しないこと、frontmatter の存在を独立した検証とする理由 (`-1` が除去範囲を持たない分類の `removalStartLine` と衝突する)、frontmatter 行数の算出を R-008 と共有すべきこと (REQ-C-001) を規定。Edge 17 を追加し Section 2.6 に DR-35、Traceability の REQ-F-008 を更新。based-on を requirements.md v8.6.0 へ更新。R-001〜R-015 の規則 ID と評価順序は不変                                                                                                                              |
| 2026-08-18 | 5.6.0   | DR-36 を反映 (MINOR: 前提を追加)。Section 2.2 Design Assumptions に「`chatlogs/` 配下へ書き込むのは本ツール群のみである」を追加し、Section 2.6 に DR-36 を追加。Section 4.2 の再検証 (DR-35) に、検証が R-009 の手順 1 の直前でありスワップ地点ではないため競合の窓が閉じないこと、その窓を単一書き手前提のもとで残余として受容すること、本検証は競合の検出ではなく安全弁として維持することを追記。based-on を requirements.md v8.7.0 へ更新。R-001〜R-015 の規則と検証 4 点は不変                                                                                                                                                                                                                                                                                                                              |
| 2026-08-19 | 5.7.0   | DR-37 を反映 (MINOR: 報告対象を追加)。Section 2.6 に DR-37 を追加。Section 3 の通常実行の per-file 報告に判定 error (R-002 / R-007) を加え、書式を `error: <path> (rule=R-NNN)` と規定。判定 error と書き込み失敗を同じ `error` 行で報告すること、`kind` / `subindex` を出力しないこと、`done` の非出力を維持すること、分岐を分類 (`outcome`) で行うことを明記。based-on を requirements.md v8.8.0 へ更新。R-001〜R-015 の規則と dry-run 明細の書式は不変                                                                                                                                                                                                                                                                                                                                                       |
| 2026-08-21 | 6.0.0   | DR-38 / DR-39 / DR-40 を反映 (MAJOR: 決定済みの受理範囲を撤回)。Section 4.1 の R-001 から入力ディレクトリの拒否を撤回し、条件を「出力ディレクトリの指定」「入力ディレクトリの指定が無いままの年月省略」の 2 条件へ改訂。入力ディレクトリ指定時は agent / period を対象の解決に用いず年月の省略も受理することを明記。R-016 (列挙直後のキャッシュキー衝突の検査) と R-017 (対象ディレクトリの再帰走査) を新設し、R-017 を本体列挙・孤立退避の検出・退避の一括削除の 3 経路へ同時適用すること、R-010 の削除範囲が対象サブツリー全体に及ぶことを規定。Section 5 の Edge 8 を実行拒否から受理へ反転し Edge 18 / 19 / 20 を追加。Section 2.6 に DR-38 / DR-39 / DR-40 を追加し Traceability に REQ-C-009 / REQ-C-010 を追加。based-on を requirements.md v9.0.0 へ更新。R-002〜R-015 の振る舞い規則は不変             |
